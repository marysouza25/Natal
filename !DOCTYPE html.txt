<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Contagem para o Natal 🎄</title>
  <style>
    body {
      background: linear-gradient(135deg, #b30000, #006400);
      color: white;
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
    }
    .box {
      background: rgba(0,0,0,0.4);
      padding: 30px;
      border-radius: 20px;
    }
    h1 {
      font-size: 2.5em;
    }
    #contador {
      font-size: 2em;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <div class="box">
    <h1>🎄 FALTAM...</h1>
    <div id="contador">Carregando...</div>
    <p>para o melhor dia do ano ✨🎅❤️</p>
  </div>

  <script>
    function atualizarContagem() {
      const natal = new Date("December 25, 2025 00:00:00").getTime();
      const agora = new Date().getTime();
      const diferenca = natal - agora;

      const dias = Math.floor(diferenca / (1000 * 60 * 60 * 24));
      const horas = Math.floor((diferenca % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutos = Math.floor((diferenca % (1000 * 60 * 60)) / (1000 * 60));
      const segundos = Math.floor((diferenc
